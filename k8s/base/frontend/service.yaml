apiVersion: v1
kind: Service
metadata:
  name: react-frontend
  namespace: microservicios-web-imagenes
  labels: { app: react-frontend }
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: react-frontend
  type: ClusterIP
